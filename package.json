{
  "name": "bazarr-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for managing Bazarr subtitles",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "bazarr-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:e2e": "docker compose -f docker-compose.test.yml up -d && vitest run; docker compose -f docker-compose.test.yml down -v"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "typescript": "^6.0.2",
    "vitest": "^4.1.4"
  }
}
